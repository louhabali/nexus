docker exec -it nexus cat /nexus-data/admin.password


// after repo creating 
# 1. Log in to your hosted storage locker
docker login localhost:8091 -u admin -p YOUR_PASSWORD

# 2. Tag your image to point to port 8091
docker tag my-app:latest localhost:8091/my-app:latest

# 3. Push it directly into Nexus
docker push localhost:8091/my-app:latest